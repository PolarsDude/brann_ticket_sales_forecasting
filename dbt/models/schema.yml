version: 2

sources:
  - name: raw
    description: Raw data from web scraping
    tables:
      - name: raw_eliteserien_results
        description: Raw Eliteserien match results (date, matchday, teams, result, snapshot_at)
